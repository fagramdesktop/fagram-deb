### Info
DEBIAN/UBUNTU APT repository for [FAgram](https://github.com/fagramdesktop/fadesktop), offering a more convenient alternative to manually replacing the binary or flatpaks.

CI is set to run automatically on every new release.

### Installation

```bash
$ bash <(curl -s "https://raw.githubusercontent.com/fagramdesktop/fagram-deb/main/install.sh")
```
